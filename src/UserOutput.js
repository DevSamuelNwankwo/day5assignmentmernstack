const UserOutput = (props) => {
    return ( <div>
        <p> Hey {props.username}  </p>
        <p> Goodday @ {props.username} </p>
        <p> How Are You Doing Today {props.username} ? </p>

    </div> );
}
 
export default UserOutput;